Test 1

Field technicians already repaired the issue, but the operational dashboard still marks some homes as problematic. What should I verify before closing the ticket?

Expected:

Defective Port Cleanup
Test 2

The customer impact is gone, however some indicators remain attached to the home records. I need to remove those traces from the database.

Expected:

Provision Cleanup
Defective Port Cleanup
Test 3

An attachment contains a list of records that should no longer exist because they were imported twice.

Expected:

Duplicate Cleanup
Test 4

The spreadsheet highlights entries that need to be removed. I want the safest approach before deleting anything manually.

Expected:

Duplicate Cleanup
Test 5

A completed deployment is still shown as pending in the planning systems.

Expected:

Home Status Change
Test 6

The status was updated successfully in the source platform but downstream tools continue showing the old value.

Expected:

COMA Synchronization
Test 7

Operations says the correction was completed days ago, but reporting still contains stale records.

Expected:

Export Verification
Cleanup Verification
Test 8

One identifier in the spreadsheet does not match what I see in the database export. How should I determine which value is trustworthy?

Expected:

Access Number Mismatch




1.  "I need to remove the P flag from the status column for several home IDs in the network inventory"
2.  "There are duplicate yellow-highlighted rows in the spreadsheet that need to be deleted from the database"
3.  "The property needs to be moved from HomePlanningPending to HomeUnplanned"
4.  "An employee left the company — need to disable their account and remove all access immediately"
5.  "The SSL certificate on our production API gateway expires in 3 days"
6.  "The Java application server is eating all available heap memory and crashing every few hours"
7.  "The /var/log partition is at 99% capacity and the server is throwing disk full errors"
8.  "Nightly database backup has not run in 5 days, no error in the UI"
9.  "Our integration with the payment provider is returning 429 errors and orders are failing"
10. "The subdomain api.internal.company.com is not resolving correctly since yesterday's DNS update"
11. "Legitimate traffic from the office IP range is being blocked — suspect firewall misconfiguration"
12. "The auth-service pod in production Kubernetes cluster keeps restarting every 2 minutes"
13. "The nightly ETL job failed at the transform step — data warehouse not updated since Tuesday"
14. "Users are getting LDAP bind failed errors when trying to log into the internal portal"
15. "The application lost access to the S3 bucket after we rotated the IAM credentials"
16. "The deployment pipeline fails at the install step after we updated the Node version in package.json"
17. "Emails sent from our domain are bouncing with a 550 relay not permitted error"
18. "The VPN between our Frankfurt office and AWS us-east-1 keeps dropping every 3-4 hours"
19. "We are getting 800+ PagerDuty alerts per hour for a metric that looks completely normal"
20. "The application logs stopped rotating and are now filling the disk — logrotate seems broken"