# ── Web framework (replaces Express server.ts) ────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9        # form/file handling if needed by routes
pydantic>=2.7.0

# ── Chatbot module (ported into app/chatbot, Streamlit removed) ───────────────
sentence-transformers>=3.0.0   # embedding model for retrieval
numpy>=1.26.0
ollama>=0.2.0                  # default LLM provider (self-hosted)
python-docx>=1.0.0             # optional: only needed to index .docx reports
Pillow>=10.0.0                 # optional: image handling for the vision path

# Optional: Gemini provider (swappable alternative to Ollama). Uncomment to use.
# google-genai>=0.3.0

# ── Data layer (Postgres catalog + MinIO blobs) ───────────────────────────────
# SQLAlchemy 2.x typed ORM. psycopg 3 is the current driver (psycopg2 is legacy);
# the [binary] extra ships wheels so no local libpq build is needed.
SQLAlchemy>=2.0.30
psycopg[binary]>=3.1.18
alembic>=1.13.0                # schema migrations, versioned in git
pgvector>=0.2.5                # ANN column type for semantic chat search
boto3>=1.34.0                  # S3 API client; MinIO is S3-compatible

# ── Auth (Keycloak OIDC) ──────────────────────────────────────────────────────
# Validates RS256 tokens against Keycloak's JWKS. cryptography is the backend
# PyJWT needs for asymmetric algorithms.
PyJWT>=2.8.0
cryptography>=42.0.0

# ── Tests ─────────────────────────────────────────────────────────────────────
pytest>=8.0.0
httpx>=0.27.0                  # FastAPI TestClient dependency
