# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Node / frontend
node_modules/
frontend/dist/

# Env
.env
.env.local

# OS / editor
.DS_Store

# Local data (reports are shared sample data; keep them tracked for now, but
# ignore anything the running app writes at these ad-hoc paths)
/data/
